# Trustin_Server

step 1: Use This Command "npm install" to Install The "node_modules" 
        If you found any trouble in Installing use the mentioned force commands to install the
        package["--force" or "--legacy-peer-deps"]

step 2: To store the environment variable create ".env" file.
        The environment are as follows
         port = 8080
         MongoDB_URL="mongodb://0.0.0.0:27017/Trustin"
         Email_User=melisa.connelly@ethereal.email
         Email_password=haxsvvfZWahrksKx1e
         TOKEN_SECRET=shaju@123randam

step 3:  To run the application use "npm start"